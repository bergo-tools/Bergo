package cli

/*
import (
	"log"
	"os"
)

func init() {
	f, err := os.OpenFile("tealog.log", os.O_RDWR|os.O_CREATE|os.O_APPEND, 0666)
	if err != nil {
		log.Fatal(err)
	}

	// 设置日志输出
	log.SetOutput(f)
}
*/
